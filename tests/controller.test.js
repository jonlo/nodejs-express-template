const { Controller } = require('../server/controllers/controller');

// Controller pool tests
// describe('Car pool integration tests', () => {

//     it('set cars works properly', async () => {
//         const controller = new ();
//         let carsData = [{ id: 0, seats: 4 }, { id: 1, seats: 6 }, { id: 2, seats: 4 }];
//         carPool.setCars(carsData);
//         expect(Object.values(carPool.cars.get()).length).toEqual(2);
//     });

// });